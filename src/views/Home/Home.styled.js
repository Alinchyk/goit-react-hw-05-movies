import styled from 'styled-components';

export const HomeTitle = styled.h2`
  color: black;
  font-family: 'Droid serif', serif;
  font-size: 36px;
  font-weight: 800;
  font-style: italic;
  line-height: 44px;
  margin: 0 0 12px;
  text-align: center;
`;
